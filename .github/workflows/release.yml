name: 'Release'

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v0.1.1)'
        required: true
        default: 'v0.1.0'

jobs:
  build-windows:
    permissions:
      contents: write
    runs-on: windows-latest

    env:
      VERSION: ${{ github.event.inputs.version || github.ref_name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史用于生成 changelog

      - name: Generate changelog
        id: changelog
        shell: bash
        run: |
          # 获取上一个 tag
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          echo "Previous tag: $PREV_TAG"

          if [ -z "$PREV_TAG" ]; then
            # 首次发布，获取所有提交
            COMMITS=$(git log --pretty=format:"- %s" -20)
          else
            # 获取两个 tag 之间的提交
            COMMITS=$(git log --pretty=format:"- %s" ${PREV_TAG}..HEAD)
          fi

          # 如果没有提交记录
          if [ -z "$COMMITS" ]; then
            COMMITS="- 无更新记录"
          fi

          echo "Changelog:"
          echo "$COMMITS"

          # 输出到 GitHub Actions
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMITS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri -> target'
          cache-on-failure: true

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri build

      - name: Rename portable exe
        run: |
          mv src-tauri/target/release/port-detection.exe src-tauri/target/release/PortDetection_${{ env.VERSION }}_x64_portable.exe

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.VERSION }}
          files: |
            src-tauri/target/release/PortDetection_${{ env.VERSION }}_x64_portable.exe
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*-setup.exe
          body: |
            ## PortDetection ${{ env.VERSION }}

            Windows 端口检测与管理工具

            ### 📋 更新内容
            ${{ steps.changelog.outputs.changelog }}

            ### 📦 下载说明
            | 文件 | 说明 |
            |------|------|
            | `_x64_portable.exe` | **便携版** - 无需安装，直接运行 |
            | `.msi` | Windows Installer 安装包 |
            | `_x64-setup.exe` | NSIS 安装程序 |

            ### 💻 系统要求
            - Windows 10/11 64-bit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
